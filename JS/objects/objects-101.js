let myBook = {
    title: 'Gates of Fire',
    author: 'Steven Pressfield',
    pageCount: 450
}

console.log(myBook)
console.log(myBook.title)

let person = {
    name: 'Dave',
    age: 31,
    location: 'London'
}

console.log(`${person.name} is ${person.age} and lives in ${person.location}`)

person.age = 32

console.log(`${person.name} will be ${person.age} next year and lives in ${person.location}`)